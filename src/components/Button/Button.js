import React from 'react';
import './Button.scss'

const Button = () => {
    return (
        <div>
            <h1>need to design a defult button</h1>
        </div>
    );
};

export default Button;